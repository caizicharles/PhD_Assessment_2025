name: HIP
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - pytables
  - matplotlib
  - scikit-learn
  - pytorch
  - pytorch-cuda
  - pip:
      - pyhealth
      - PyYAML
      - tqdm
      - mlflow